// Semi-Colons (;) are required in rust.

// Create connections to the files
mod print;
mod variables;
mod types;
mod strings;

fn main() {
    // Calling the functions we create in seperate files
    // print::run();
    // variables::run();
    // types::run();
    strings::run();
}
